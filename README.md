# HashChecker - 文件校验和计算工具

一个简单易用的 Windows 桌面应用程序，用于计算文件的校验和（哈希值），支持多种哈希算法。

## 功能特性

- 📁 选择任意文件进行校验和计算
- 🔐 支持多种哈希算法：
  - MD5
  - SHA1
  - SHA256
  - SHA384
  - SHA512
- 📋 一键复制校验和结果到剪贴板
- 🎨 简洁直观的用户界面

## 技术栈

- .NET 10.0
- Windows Forms
- C# 10.0

## 环境要求

- **操作系统**: Windows 10 或更高版本
- **运行时**: .NET 10.0 Runtime（如果使用独立发布的版本则无需安装）

## 快速开始

### 运行项目

如果您已安装 .NET SDK，可以直接运行：

```bash
dotnet run
```

### 编译项目

```bash
dotnet build -c Release
```

编译后的可执行文件位于：`bin\Release\net10.0-windows\HashChecker.exe`

## 打包发布

### 方式一：单文件独立发布（推荐）

将应用程序及其所有依赖打包成单个 exe 文件，包含 .NET 运行时，无需目标机器安装 .NET：

```bash
dotnet publish -c Release -r win-x64 --self-contained true /p:PublishSingleFile=true
```

生成的 exe 文件位于：`bin\Release\net10.0-windows\win-x64\publish\HashChecker.exe`

**优点：**
- 单个 exe 文件，分发方便
- 目标机器无需安装 .NET 运行时
- 双击即可运行

**缺点：**
- 文件体积较大（约 60-80 MB，因为包含完整运行时）

### 方式二：单文件依赖框架发布

不包含 .NET 运行时，文件体积更小，但目标机器需要安装 .NET 10.0：

```bash
dotnet publish -c Release -r win-x64 --self-contained false /p:PublishSingleFile=true
```

### 其他发布选项

**发布到其他平台：**
```bash
# Windows 32位
dotnet publish -c Release -r win-x86 --self-contained true /p:PublishSingleFile=true

# Windows ARM64
dotnet publish -c Release -r win-arm64 --self-contained true /p:PublishSingleFile=true
```

**使用 ReadyToRun (AOT) 提升启动速度：**
```bash
dotnet publish -c Release -r win-x64 --self-contained true /p:PublishSingleFile=true /p:PublishReadyToRun=true
```

## 使用说明

1. **选择文件**
   - 点击"选择文件"按钮
   - 在文件对话框中选择要计算校验和的文件

2. **选择算法**
   - 从下拉菜单中选择哈希算法（默认 SHA256）

3. **计算校验和**
   - 点击"计算"按钮
   - 等待计算完成，结果将显示在结果框中

4. **复制结果**
   - 点击"复制"按钮将校验和复制到剪贴板

## 项目结构

```
HashChecker/
├── Form1.cs              # 主窗体逻辑
├── Form1.Designer.cs     # 窗体设计器生成的代码
├── Program.cs            # 程序入口
├── HashChecker.csproj    # 项目配置文件
├── README.md             # 项目说明文档
└── LICENSE               # MIT 许可证
```

## 开发

### 环境配置

确保已安装以下工具：
- [.NET SDK 10.0](https://dotnet.microsoft.com/download)
